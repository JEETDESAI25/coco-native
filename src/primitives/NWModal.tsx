/**
 * Styled Nativewind Modal Component
 */

import {styled} from 'nativewind';
import Modal, {ModalProps} from 'react-native-modal';

const NWModal = styled<ModalProps>(Modal);
export default NWModal;
